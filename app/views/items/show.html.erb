<div class="container mt-3">
  <h2><%= @item.name %></h2>
  <%= cl_image_tag @item.photo.key %>
  <p><%= @item.description %></p>
  <p>Quanity: <%= @item.quantity %></p>
  <%= link_to sanitize("<i class='fas fa-arrow-alt-circle-left'></i>"), @item.warehouse, class: "back-btn m-2"%>
</div>
